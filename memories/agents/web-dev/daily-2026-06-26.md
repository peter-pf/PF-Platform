# web-dev — 2026-06-26

Invocation: daily-report rebuild + fleet/lists

## Field Ops daily-report rebuild + field-lists.js (NOT committed; Peter ships)
- NEW functions/api/field-lists.js: KV field_lists_v1. GET field_ops (crew reads dropdowns); POST owner-only (isPrivileged in-handler 403 for field_ops/BD). Seed defaults on empty KV. foremen forced subset of personnel. No fetch/mail/$. ownedEquipment+personnel+foremen editable; rental+maintenance categories FIXED.
- daily-report.js: +equipmentOwned/equipmentRental/maintenance/futureIssues (hours()=operating hours, no $). cleanX builders + caps. futureIssues persists for Phase 2.
- auth.js: /api/field-lists -> field_ops area.
- index.html: rebuilt mod-daily-reports (8 items: project type-filter, foreman select prefills crew row1, owned/rental rows +Add, 5 fixed maintenance rows, future issues). NEW mod-field-lists owner-only mgmt UI under PF Admin (nav data-fo-hide; editor admin/partner only, BD read-only).
- SELF-CHECK: test-rbac 687/0 (+26), classification PASS, node --check clean (endpoints + both inline modules), zero em dashes in my changes. Seed values provisional (Derek edits via UI).
- PHASE 2 (not built): compiled maintenance dashboard aggregating futureIssues[] by equipment.

## QA round-2 fixes (NOT committed; Peter ships)
- DEF-2 (data corruption): hours() 24-cap was reused for equipment hour-meter readings (1450 stored as 24). Added meterHours() (no 24 cap, abuse cap 1e6, neg rejected); switched equipmentOwned/equipmentRental.hours + maintenance.hourAtFailure to it. crew.hours still 24-capped hours().
- DEF-5: collectBody no longer fabricates projectId from pname.split(' ')[0]; free-typed project stores projectName + empty projectId (server accepts either).
- DEF-3: opts() injects a non-empty selected value if missing from the list, so a prefilled foreman in crew row 1 never silently drops.
- SELF-CHECK: test-rbac 692/0 (+5), classification PASS, node --check clean, zero em dashes in my changes. Dry-trace: meterHours(1450)=1450, hours(1450)=24, free-typed projectId='', foreman-not-in-list still selected.
- DEFER (noted, not built): edit saved report into form, seeded banner, per-row Remove in daily form.

## Actively Pricing due-dates calendar (Jonathan) — built, NOT committed/deployed
- platform/index.html: read-only month calendar at TOP of Actively Pricing (actively_bidding mount, first in module). Derives from window.PF_PRECON, per discipline.
- Events: Prelim=Design Completed Date, Bid=Due Date (actively_bidding/submitted), Budget=Due Date (budget_pricing). Effective bucket via overrideFor relocation; dead/awarded/feasibility excluded. Dedupe resolveKey+effbucket.
- calParseYMD tolerant (ISO + MM/DD/YYYY + M/D/YY), local Y/M/D, no off-by-one. Month nav + Today + day-detail + "+N more". Monochrome + amber, shape/label tags, NO financials, esc everywhere.
- Self-check: RBAC 717/0, classification 5/5, em-dash 0, node --check OK, dry trace all assertions pass.
- Peter to triple-check, deploy --branch main (production), commit.

## Budget Pricing precon mirror (Jonathan) - DONE, not committed/deployed
- index.html only: budget_pricing now mirrors actively_bidding identity.
- subLineKey=City/State (drop the col), date label Bid Due->Budget Submission (same Due Date feed), ADD Follow Up Date col, multi-GC gclist+gcEditor (reuses precon-bid-meta, no endpoint change), Bid Total Value stays money/feed.
- KEY: hoisted gcEditor append out of detailGroups-only branch so flat-layout budget_pricing renders it; suppressed GC feed keys from flat detailCols to avoid dup.
- Self-check: test-rbac 753/0, classification 5/5, em-dash 0, inline block node --check OK, trace verified order.
- Peter triple-checks + deploys --branch main + commits.
