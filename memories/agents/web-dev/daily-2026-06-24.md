# web-dev — 2026-06-24

Invocation: dashboards | Task: Precon + BD section dashboards reusing PFDashboard engine

## Done
- Reused window.PFDashboard (added render() opts arg + mount() helper; PF dash unchanged).
- Precon Dashboard: sync/build-precon-dashboard.py -> data/precon-dashboard.js from precon-pipeline.js.
- BD Dashboard: sync/build-bd-dashboard.py -> data/bd-dashboard.js from PF_BD_Master.xlsm (bd-master.json is degenerate, only 2 sheets).
- RBAC: auth.js DATA_FILE_AREAS (precon->preconstruction, bd->business_dev). 368 test-rbac.mjs assertions pass, field_ops blocked.
- Wired both builders into tools/platform_sync_boop.sh (non-fatal). Deployed. All 4 feeds 401 no-creds.
- Commit cd07f67 pushed origin/website-build-20260609.

## Remember
- bd-master.json = NOT the real BD data. Use sync/downloads/PF_BD_Master.xlsm (header row 4, name col C, Interactions has the only dates).
- Opportunities Status/Stage are blank; Orgs/Contacts have no date-added -> omitted Won/Lost + New-X-Added honestly.
- Year auto-pick must use MOST-data-points not max() (a 2028 typo hijacked it).
- Detail: .claude/memory/agent-learnings/web-dev/precon-bd-section-dashboards-20260624.md

## 2026-06-24 (round 2) — review fixes on cd07f67
- FIX1: render() opts.note -> banner shows disclosed proxy (precon Date Submitted) + BD omissions to USER. Company PF dash passes no note (unchanged).
- FIX2: rollup() pooled-ratio for pct metrics with basisNum/basisDen. Win Rate annual now 46.67% (14/30) not 45.68% mean. Company dash margins keep avg path - proven byte-identical Annual+Q1-Q4.
- Parity: both builders compute isTemplateData via sig() check.
- test-rbac.mjs 380 pass/0 fail (+12). Deploy OK, gate 401 on / + both feeds. Commit 354ae7e pushed.
- Key: rollup basis sums use qtyActual (count metrics). Engine extracted to /tmp harness to prove pooled math without browser (no Playwright).

## 2026-06-24 (round 3) — BD CRM Companies & Contacts
- Ingest: build-bd-records.py -> data/bd-records.js (263 companies, 399 contacts, fields=row4). Links contacts by Organization==Name.
- Write-back: /api/bd-interaction (per-entity log) + /api/bd-record (overlay add company/contact), KV PF_SCHEDULE, business_dev gated. Copied pipeline-state.js pattern.
- UI: mod-bd-companies master-detail, all via window.esc.
- KEY technique: tested REAL Function handlers headlessly (import onRequestGet/Post, Map KV as env.PF_SCHEDULE, session in context.data). 15/15 pass: BD persists+reads, field_ops/no-session=403, denied=no KV write.
- test-rbac.mjs 424 pass. Deploy OK, gate 401 on feed + both endpoints. Commit 12b844d.
- Detail: .claude/memory/agent-learnings/web-dev/bd-crm-companies-contacts-20260624.md

## 2026-06-24 (round 4) — BD CRM review fixes (c3dbdba)
- FIX1: render unlinkedContacts as synthetic __unlinked__ group (was invisible). GREP feed keys against index.html next time.
- FIX2: matcher second pass (suffix-strip + startswith@wordboundary), single-match-only, fuzzyLinks audit, ambiguousCandidates. 378 linked / 21 unlinked.
- FIX3: collision-only unique ids (seed#N for dups), builder hard-verifies. 662 ids unique. 4 dup contacts now distinct.
- test-rbac.mjs 431 pass. Deploy OK, gate 401 all. Commit c3dbdba.

## 2026-06-24 (round 13) — Actual vs Budget per-month columns (d620378)
- Reworked compare table: each checked month = own Actual+Budget column group (calendar order), values straight from dataset.months[Month].rows, NO sum. Two-row thead. Kept checkboxes/mode button. No delta.
- LESSON: "side by side" means columns-per-selection reading source rows, not a rollup sum.
- test-rbac 661. Jan+Feb+Mar each cell = month's own value (12/$1M not summed), pct per-month. Precon/BD untouched. Deploy OK, gate 401.

## 2026-06-24 (round 14) — Actual vs Budget cross-year (14f3ab4)
- AUDIT: only the PF Dashboard tab is a monthly KPI source (2026). WIP tabs are project lists, not KPI. 2024/2025 = no source -> empty, lit up later.
- Feed: years map {2024 empty,2025 empty,2026 pop}; top-level unchanged. Compare = year x period cross-product (year multi-select + months + Annual); month=direct, Annual=single-year 12mo rollup sum; empty combos omitted; year asc/Annual last. Kept dividers/budget/no-delta.
- test-rbac 661. {2026}x{Annual}=144/$12M=manual. Precon/BD untouched. Deploy OK, gate 401.
