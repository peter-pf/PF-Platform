# web-dev -- 2026-06-01

Invocation: session | Task: Build budgeting and invoicing IIFE modules

## What I Did
- Built budgeting.js (Budgeting & Forecasting module) with 4 sections:
  - Revenue Forecast: FY26 $6M target, run rate from LIVE_BIDS awarded/completed, pace progress bar
  - Break-Even Analysis: $35,198/mo fixed costs, 17% contribution margin, $2.48M break-even, cost breakdown table
  - Capacity Model: per-rig calculations, 1-4 rig table with utilization at 85%/70%, 3-rig utilization bar
  - Cash Flow Summary: monthly inflow/outflow/net stats, 13-week rolling forecast table
- Built invoicing.js (Invoice Tracking module) with 5 sections:
  - AR Summary Stats: total billed/collected/outstanding/retainage/DSO with progress bar
  - Invoice Tracker Table: auto-generates 1-2 invoices per project from LIVE_PROJECTS data
  - AR Aging Buckets: 0-30/31-60/61-90/90+ with visual progress bars
  - Retainage Tracker: per-project retainage with release status
  - New Invoice Form: project dropdown, auto-retainage calculation, submit/clear buttons

## Key Patterns
- All existing modules use IIFE pattern: `(function(){ 'use strict'; ... })();`
- LIVE_PROJECTS fields: project_number, name, subcontract_value, paid, unpaid, retain_pct, retainage, work_pct_complete, gc_name, gc_pm_name, gc_pm_email, projected_start, duration_days
- LIVE_BIDS fields: number, name, city_state, bid_status, bid_value, gc_name, gc_contact, gc_email, engineer
- Platform CSS classes used: card, card-header, card-title, card-subtitle, stat-card, stat-label, stat-value, stat-change (up/down), table-wrap, badge-status (bid/pending/no-bid/review), btn, btn-primary, btn-secondary, form-group, form-label, form-input, form-select, grid, grid-2, grid-3, grid-4, progress-bar, progress-fill (green/amber/red/accent)
- Modules need corresponding `<div class="module-view" id="mod-XXX"><div id="XXX-app"></div></div>` in index.html
- Modules also need sidebar nav entries and script tags in index.html

## For Next Time
- index.html needs mod-budgeting and mod-invoicing divs + sidebar entries + script tags to wire these up
- Invoice form submit is console-log only -- needs backend integration when ready
- DSO calculation uses invoice age average as proxy; real DSO needs actual payment dates
- FY start hardcoded to Oct 2025 -- update if PF fiscal year changes
