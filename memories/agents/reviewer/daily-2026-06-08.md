# reviewer — 2026-06-08
Reviewed pricing.html Leaflet map IIFE + index.html interactive-iframe wiring. APPROVED, zero defects.
Pattern: data-driven map embeds guard PF_PRICING presence, typeof L, coord type+isFinite filter, empty-array, maxAct=1 floor (divide-by-zero). XSS-safe via esc() on loc/vendors/carriers; numbers interpolated raw = safe. esc() doesn't escape quotes — safe only in text positions, not attributes. Interactive iframes load via real src (index.html:1321-1324); non-interactive via fetch->srcdoc.
