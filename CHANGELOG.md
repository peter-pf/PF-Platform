# Changelog

All notable changes to the **PF Operations Platform** (`pf-platform.pages.dev`).

Format follows [Keep a Changelog](https://keepachangelog.com/en/1.1.0/). Versioning is
[SemVer](https://semver.org/) — until Production v1.0.0 the platform stays in the `0.x` line
with a release-stage suffix (see `docs/PLATFORM-RELEASE-STAGES.md` and `docs/devops/RELEASE-PROTOCOL.md`).

**Current stage: Alpha — entering UAT.**

---

## [Unreleased]

### Added
- Project Closeout section rebuilt (display-only, top-level office section #7): the Project Closeout card now carries 5 subsections — Field Closeout Procedure, Send Final As-Builts to Garbin, GGG Returns Final As-Builts, Certified Payroll Reports, Thank-You Email to GC. The Certified Payroll subsection auto-reads the Contract Recap "Certified Payroll?" value (display-only cross-section read, no write). "Thank-You Sent By" field present. All state persists via the closeout override key. Change = `index.html` ONLY (display layer; the committed `portal_uploads/closeout-verify/render-proof.mjs` verify artifact was excluded from the deploy to keep production index-only). Shipped from `project-closeout-20260807` @ `ea55802` (base `section5-site-readiness-20260807` = prior prod canonical `64a8d8d4`, git tip `f34c9cd`; `git merge-base --is-ancestor f34c9cd ea55802` = YES, plus `d5db649` and `85f4499` both ancestors = YES, so no Site-Readiness / Project-Photos / Design-Professionals / Engineering / Contract Recap regression). New prod canonical `df8c3b3b` (env=production, branch=main, 2026-08-07T19:38:11Z). Prod auth fully gated post-deploy (all five checked, none open/500): `/api/me` 401, `/` 401 (Basic realm), `/api/contacts` 401, `/api/project-override` 401, `/api/project-photos` 401. Verified against shipped committed bytes: "Dead Projects"/"Project Summary"/"PF Project Schedule" present, "Award to PM Handoff"/"mod-pm-handoff" absent; 5 closeout subgroup headers present (Field Closeout Procedure / Send Final As-Builts to Garbin / GGG Returns Final As-Builts / Certified Payroll Reports / Thank-You Email to GC); "Thank-You Sent By" present; office top-level list gap-free 1-7 (General Info=1, Subcontract Agreement=2, Engineering & Design=3, Site Readiness=4, Project Photos=5, Financials=6, Project Closeout=7); prior features intact (`functions/api/contacts.js` in tree + `findDuplicate` 2×, `functions/api/project-photos.js` in tree + `pfPhotoLightbox` 3×, `PF_FIELD_TEXTAREA_LABELS` 5× + `pfAutoBackfeedDP` 2×, `pfFmtPhone` 16×, `design_professionals` 12×, Site Readiness 6 subsections Shop Drawings/Testing/Staking & Layout/Safety/Mobilization Preparation/Mobilization to Site, Project Photos section, standalone Project Safety office section gone). Real bids current on prod = 166 filtered (shipped production-current data 190 raw / 186 LIVE_BIDS, NOT the stale branch snapshot). Clean-deploy dance: dirty data/infra files stashed `-u` before checkout, production-current data overlaid onto the closeout checkout so the display layer is new while bid/financial data stays current, restored after (stash popped clean, 22 entries identical before/after). STOP-sentinel (`/home/aiciv/tools/.budget-actuals-daemon-STOP`, 0 bytes, mtime Jul 30 14:18) untouched; `_middleware.js`, `functions/api/*`, financials/QAQC/daemon/bid-data files untouched. First deploy `5c6d32fd` shipped the stale branch data snapshot (162 LIVE_BIDS); corrected immediately with `df8c3b3b` (current data + closeout display). Rollback target `64a8d8d4` held ready (not needed — both gates passed).
- Project Photos zoom lightbox live (display-only, in-page zoomable viewer): the office/field Project Photos gallery (section #6) now opens each thumbnail in an in-page zoomable lightbox (zoom / pan / close / prev-next) instead of a new browser tab. Vanilla JS/CSS, no Function changes — the `/api/project-photos` contract is untouched (read-only SharePoint gallery unchanged). Change = `index.html` ONLY (+378/-11; only file in `git diff --stat 3a5800c 0872371`). Shipped from `photos-lightbox-20260807` @ `0872371` (base `editor-improvements-20260807` = prior prod canonical `c1437587`, git tip `3a5800c`; `git merge-base --is-ancestor 3a5800c 0872371` = YES, `5585972` and `85f4499` structural rename both ancestors = YES, so no Editor-improvements / Project-Photos / Design-Professionals / Engineering / Contract Recap / Bid Recap / contact-directory / section regression). New prod canonical `2b90a1b9` (env=production, branch=main, 2026-08-07T19:06:54Z). Prod auth fully gated post-deploy (all five checked, none open/500): `/api/me` 401, `/` 401 (Basic realm), `/api/contacts` 401, `/api/project-override` 401, `/api/project-photos` 401 (read-only gallery route still gated). Verified against shipped committed bytes (index.html was the ONLY changed file, no build/transform step): "Dead Projects"/"Project Summary"/"PF Project Schedule" present, "Award to PM Handoff"/"mod-pm-handoff" absent, prior features intact (`functions/api/contacts.js` in tree + `findDuplicate` 2×, Design Professionals feature across `contacts.js`/`project-override.js`/`index.html`, `functions/api/project-photos.js` in tree, `PF_FIELD_TEXTAREA_LABELS` 5× + `pfAutoBackfeedDP` 2×, `pfFmtPhone` 16×, Contract Recap 4 subgroups, Engineering 3 subsections, Project Photos section #6, office sections renumber gap-free 1-8), this build's markers present (`pfPhotoLightbox` 3×, `pf-lb-stage` 5×, `clampPan` 5×, old new-tab `class="pr-photo" href=` links = 0), 166 real bids (committed `bid-log.json`). STOP-sentinel (`/home/aiciv/tools/.budget-actuals-daemon-STOP`) untouched (0 bytes, mtime Jul 30 14:18, before + after; main-branch deploy, never a budget-actuals invocation), dirty data/sync files stashed `-u` before deploy + restored after (stash popped clean, index/functions never dirty). Rollback target `c1437587` held ready (not needed — both gates passed).
- Editor improvements live (multi-line textarea fields + DP contacts auto-write to master): the office Project Record gains a reusable multi-line editor mechanism (`PF_FIELD_TEXTAREA_LABELS` = 5×) that renders labeled fields as multi-line `textarea`s with a `pr-field-value-multiline` display (4×) — wired to "Extra Scope of Work Language" and "Field As-Built Notes" so free-form entries preserve line breaks. Every project-record save now auto-backfeeds the General Info Design Professionals contacts into the master contact directory Excel via a best-effort, non-blocking, duplicate-guarded write (`pfAutoBackfeedDP` = 2×, `pfCollectDirectoryContacts` = 3×) — the save UX never blocks on the backfeed and dup-guards prevent duplicate master rows. Change = `index.html` ONLY (+180/-34; disk `index.html` byte-identical to the shipped commit, blob `fe51ac5f` == `5585972:platform/index.html`, verified before + after the data stash). `contacts.js` reused byte-unchanged (base `0fde27f:...contacts.js` == tip `5585972:...contacts.js`, blob `02c51c03`). A test asset ships as a static file alongside the existing `portal_uploads/` verify files (`portal_uploads/editor-improvements-verify/render-proof.mjs`, no secrets, same class as `dp-editable-verify`/`proj-override-verify`). Shipped from `editor-improvements-20260807` @ `5585972` (base `project-photos-20260807` = prior prod canonical `1fcf2b45`, git tip `0fde27f`; `git merge-base --is-ancestor 0fde27f 5585972` = YES, `227f196` and `85f4499` structural rename both ancestors = YES, so no Project-Photos / Subcontract / Design-Professionals / Engineering / Contract Recap / Bid Recap / contact-directory / section regression). New prod canonical `c1437587` (env=production, branch=main, 2026-08-07T18:57:28Z). Prod auth fully gated post-deploy (all five checked, each body "Authentication required", none open/500): `/api/me` 401, `/` 401 (Basic realm), `/api/contacts` 401, `/api/project-override` 401, `/api/project-photos` 401. Verified against shipped committed bytes: "Dead Projects"/"Project Summary"/"PF Project Schedule" present, "Award to PM Handoff"/"mod-pm-handoff" absent, prior features intact (`functions/api/contacts.js` in tree byte-unchanged + `findDuplicate`, `functions/api/project-override.js` `design_professionals` key, `functions/api/project-photos.js` in tree, `card(6, 'Project Photos'` = 1, `pfFmtPhone` 16×, Contract Recap 4 subgroups, Engineering 3 subsections, QA/QC relocated, office sections renumber gap-free 1-8 with Project Photos=6), this build's markers present (`PF_FIELD_TEXTAREA_LABELS` 5×, `pfAutoBackfeedDP` 2×, `pfCollectDirectoryContacts` 3×, `pr-field-value-multiline` 4×), 166 real bids (committed `bid-log.json`). STOP-sentinel (`/home/aiciv/tools/.budget-actuals-daemon-STOP`) untouched (0 bytes, mtime Jul 30 14:18:04.921114446, before + after; main-branch deploy, never a budget-actuals invocation), dirty data/sync files stashed `-u` before deploy + restored after (stash popped clean, index/functions never dirty). Rollback target `1fcf2b45` held ready (not needed — both gates passed).
- Project Photos gallery + PF Project Team card title fix live: a new "Project Photos" section (project detail card #6, directly below Site Readiness) renders a read-only SharePoint photo gallery for the project, and the PF Project Team nested card now titles each member name-first with the role shown as a tag and an override-aware contact name. Ships THREE files: new `functions/api/project-photos.js` (read-only Microsoft Graph gallery, area-gated `field_ops`, reuses the untouched `functions/lib/graph.js` helper — no write path, no financials); `functions/lib/auth.js` gains one route classification (`/api/project-photos` -> `field_ops`) so the new endpoint is auth-gated (admin + field_ops + partner scope per that area, never open); and `index.html` adds the `card(6, 'Project Photos', ...)` section + `wireProjectPhotos` gallery JS/CSS, the PF Project Team card title fix (name-first title via `ovLookup('pfTeam', role + ' - Contact Name')` so a stored override name wins, role rendered as a tag), and renumbers the office project-detail sections gap-free 1-8 (Project Photos=6, Financials=7, Project Closeout=8). Shipped from `project-photos-20260807` @ `227f196` (base `subcontract-tax-tweaks-20260807` = prior prod canonical `94640440`, git tip `fb92e82`; `git merge-base --is-ancestor fb92e82 227f196` = YES, `c21a02c` feature tip and `85f4499` structural rename both ancestors = YES, so no Subcontract / Design-Professionals / Engineering / Contract Recap / Bid Recap / contact-directory / section regression). New prod canonical `1fcf2b45` (env=production, branch=main, 2026-08-07T18:40:59Z). Prod auth fully gated post-deploy (Function-change build, all five checked): `/api/me` 401, `/` 401 (Basic realm "PF Operations Platform"), `/api/contacts` 401, `/api/project-override` 401, `/api/project-photos` 401 (new route gated — body "Authentication required", not 500/open). Verified against shipped committed bytes (disk `index.html` byte-identical to `227f196:platform/index.html`, blob `91716955`, `project-photos.js` blob `c2c47730`, `auth.js` blob `5bcdcc3c`, canonical=production confirmed): "Dead Projects"/"Project Summary"/"PF Project Schedule" present, "Award to PM Handoff"/"mod-pm-handoff" absent, prior features intact (`functions/api/contacts.js` in tree, `project-override.js` `design_professionals` key + `contacts.js` `findDuplicate`, `pfFmtPhone` 17×, Contract Recap 4 subgroups, Engineering 3 subsections, QA/QC relocated, Bid Recap "Released to Start Submittals", Sales Tax dropdown + "Sales Tax %" + "Sales Tax Notes", To Be Placed = 4 fields), this build's markers present (`functions/api/project-photos.js` in tree, `card(6, 'Project Photos'` = 1, `wireProjectPhotos` present, pfTeam title fix `ovLookup('pfTeam', role + ' - Contact Name')`, office sections renumber gap-free 1-8 with Project Photos=6 / Financials=7 / Project Closeout=8), 166 real bids. STOP-sentinel (`/home/aiciv/tools/.budget-actuals-daemon-STOP`) untouched (0 bytes, mtime Jul 30 14:18:04, before + after; main-branch deploy, never a budget-actuals invocation), dirty data/sync files stashed `-u` before deploy + restored after (stash popped clean). Rollback target `94640440` held ready (not needed — both gates passed).
- Editable Design Professionals + master-Excel backfeed live: the General Info "Design Professionals" block becomes per-project editable and its contact edits backfeed into the master contact directory Excel. Ships THREE files: `functions/api/project-override.js` gains one allowlist key (`design_professionals: 1`) so the DP section can round-trip a project override; `functions/api/contacts.js` gains an additive duplicate-guard (`findDuplicate`) so a backfed contact does not create a duplicate row in the master list; and `index.html` gives the DP block an Edit path with the `design_professionals` sectionKey wired to backfeed. Both Function changes are additive and stay auth-gated (no new unauth path). Shipped from `dp-editable-backfeed-20260807` @ `c4b4406` (base `eng-design-subsections-20260807` = prior prod canonical `294e99eb`; `git merge-base --is-ancestor a6e095e c4b4406` = YES, `115286a` feature tip and `85f4499` structural rename both ancestors = YES, so no Engineering / Contract Recap / Bid Recap / contact-directory / section regression). New prod canonical `24b966b0` (env=production, branch=main, 2026-08-07T18:12:20Z). Prod auth fully gated post-deploy (Function-change build, all four checked): `/api/me` 401, `/` 401 (Basic realm "PF Operations Platform"), `/api/contacts` 401 (dup-guard addition did NOT open the gate — not 500/open), `/api/project-override` 401 (new allowlist key did NOT open an unauth path — GET and POST both 401, not 500/open). Verified against shipped committed bytes: "Dead Projects"/"Project Summary"/"PF Project Schedule" present, "Award to PM Handoff"/"mod-pm-handoff" absent, prior features intact (`functions/api/contacts.js` in tree, `pfFmtPhone` 17×, Contract Recap 4-group hierarchy, Bid Recap fields, Engineering 3 subsections "Prelim Design"/"PF Design Submittal"/"As Built Drawings", QA/QC relocated into Engineering 43×), this build's markers present (`project-override.js` `design_professionals: 1` in allowlist, `contacts.js` `findDuplicate` dup-guard, `index.html` `design_professionals` sectionKey + DP Edit path), 166 real bids. STOP-sentinel (`/home/aiciv/tools/.budget-actuals-daemon-STOP`) untouched (main-branch deploy, never a budget-actuals invocation), dirty data/`_headers`/sync files stashed `-u` before deploy + restored after. Rollback target `294e99eb` held ready (not needed — both gates passed).
- Bid Recap milestone fields live: the Subcontract Agreement > Bid Recap subsection gains two new display-layer fields — "Released to Start Submittals" (a date milestone with native date-picker via `PF_FIELD_DATE_LABELS` + `pfToDateInputValue`, rendered MM/DD/YYYY) and "Bid Sent to GC" (a clickable, per-project-editable SharePoint link via `fieldLinked(..., 'website')`, blank until set). Additive display layer only (`index.html` +62/-3, two commits `94ccd36` date field + `9a87eea` link field), no Functions, no data mutation, no auth touch. Shipped from `bid-recap-released-submittals-20260807` @ `9a87eea` (base `subcontract-recap-rework-20260807` = prior prod canonical `497959e9`; `git merge-base --is-ancestor 304d1ba 9a87eea` = YES, `bd93a1f` feature tip and `85f4499` structural rename both ancestors = YES, so no section/Contract-Recap regression). New prod canonical `66e3870a` (env=production, branch=main, 2026-08-07T17:11:23Z). Prod auth fully gated post-deploy: `/api/me` 401, `/` 401 (Basic realm), `/api/contacts` 401 (contact directory Function still deployed + CRM-gated, body "Authentication required", not open/500). Verified against shipped bytes: "Dead Projects"/"Project Summary"/"PF Project Schedule" present, "Award to PM Handoff"/"mod-pm-handoff" absent, prior features intact (`functions/api/contacts.js` + `/api/contacts` + "Save contact(s) to directory", `pfFmtPhone` 17×, Contract Recap `twoCol` + "Project Substantial Completion Date", "Agreement Date (Face)" gone), new markers present ("Released to Start Submittals" 3×, "Bid Sent to GC" 2×, `PF_FIELD_DATE_LABELS` 4×, date-picker), Bid Recap render order Bid Recap subhead -> Released to Start Submittals -> Bid Sent to GC -> Contract Recap subhead, 166 real bids.
- Portal-wide phone-number display formatting live: new `pfFmtPhone` display helper in `index.html` normalizes stored phone values to `(111) 111-1111` at render time, label-gated so only phone fields are reformatted, and keeps numbers clickable via `tel:`. Display-only cosmetic change (same class as the live date formatter) — no Functions, no data mutation, no auth touch. Shipped from `phone-format-20260807` @ `99aebf3` (base `contact-directory-20260807` @ `18035e5` = the prior prod canonical `e1e3b3da`; `git merge-base --is-ancestor 18035e5 99aebf3` = YES, and `85f4499` structural rename is an ancestor = YES, so no section regression). Change = `index.html` only (`pfFmtPhone` present 16× in shipped bundle); test file `sync/pffmtphone_test.js` not shipped (rsync excludes `sync/`). New prod canonical `6b951da7` (env=production, branch=main, 2026-08-07T13:53:47Z). Prod auth fully gated post-deploy: `/api/me` 401, `/` 401 (Basic realm "PF Operations Platform"), `/api/contacts` 401 (contact directory Function still deployed + CRM-gated, not open/500). Structure verified against shipped bytes: "Dead Projects"/"Project Summary"/"PF Project Schedule" present, "Award to PM Handoff"/"mod-pm-handoff" absent, contact directory (`functions/api/contacts.js` + `/api/contacts` + "Save contact(s) to directory" markers) intact, 166 real bids.
- Master contact directory wiring live: new `functions/api/contacts.js` (read + write-back to PF Master Contact List.xlsx, GET+POST both `requireArea('crm')`), `functions/lib/auth.js` classifies `/api/contacts` as the `crm` area (admin + partner + business_dev; field_ops blocked), and `index.html` gains contact typeahead + a "Save contact(s) to directory" write-back button. Carries no financials (names/titles/companies/phones/emails only). Shipped from `contact-directory-20260807` @ `bf27b2a` (base `general-info-rework-20260806` @ `8eae0af` = current prod canonical structure; `git merge-base --is-ancestor 8eae0af bf27b2a` = YES). Passed adversarial code review (GO, no blockers). New prod canonical `e1e3b3da` (env=production, branch=main). Prod auth fully gated: `/api/me` 401, `/` 401 (Basic realm), `/api/contacts` 401 (deployed + CRM-gated, not open/500).
- DevOps function: team charter, release protocol, logging/monitoring runbook, and maintenance/support runbook under `docs/devops/`, plus this changelog. Formalizes how changes ship, get logged, and stay recoverable.

### Changed
- Site Readiness / Project Setup restructure live (6 subsections + Project Safety merge + standalone Section 4 removal + gap-free renumber): the office Site Readiness / Project Setup section is reorganized into six blue-headered `subgroup()` sub-sections — "1. Shop Drawings", "2. Testing", "3. Staking & Layout", "4. Safety", "5. Mobilization Preparation", "6. Mobilization to Site" — and the standalone "Project Safety" section (former office card #4) is REMOVED as a top-level card with its fields (SSSP, Safety Consultant, MSDS, Toolbox Talks, JSA, Hand Log Sheets, Locate Ticket, etc.) absorbed into the new Safety subsection. The `safety` override key is PRESERVED and rendered as a NESTED editable card injected post-render via `_surfaceExtras(root.querySelector('.pr-card[data-pr-section="safety"] .pr-card-body'), 'safety', ...)` (same idiom as the existing Equipment/Material nested cards) so ALL prior safety data + its Edit/Save plumbing survive with ZERO migration; the old flat siteReadiness rows are surfaced into a "To Be Placed" group so no stored data is orphaned. Equipment/Material fields are folded into subsection 6 (their `equipment`/`material` override keys preserved). With the standalone Project Safety card gone, the office sections renumber gap-free 1-7: General Info=1, Subcontract Agreement=2, Engineering & Design=3, Site Readiness / Project Setup=4, Project Photos=5, Financials=6, Project Closeout=7. Every subgroupNote describing extraction / reminders / notify / upload-integration is a NOTE only — no automation is wired in this build. Field-companion renderer (its own `card(5, 'Project Safety')` at ~L22559) is a separate render surface, out of scope and preserved unchanged. `index.html` display layer only (+330/-43 vs base; +373/-43 committed with the render-proof helper). Shipped from `section5-site-readiness-20260807` @ `d5db649` (base `photos-lightbox-20260807` = prior prod canonical `2b90a1b9`, git tip `232fe50`; `git merge-base --is-ancestor 232fe50 d5db649` = YES, `0872371` and `85f4499` structural rename both ancestors = YES, so no Photos-lightbox / Project-Photos / Design-Professionals / Engineering / Contract Recap / Bid Recap / contact-directory / section regression). New prod canonical `64a8d8d4` (env=production, branch=main, 2026-08-07T19:22:01Z). Prod auth fully gated post-deploy (all five checked, none open/500): `/api/me` 401, `/` 401 (Basic realm "PF Operations Platform"), `/api/contacts` 401 (body "Authentication required"), `/api/project-override` 401, `/api/project-photos` 401. Verified against shipped committed bytes (disk `index.html` byte-identical to `d5db649:platform/index.html`, blob `beee3c3a`, verified before + after the data stash, canonical=production confirmed via project pointer not list order): "Dead Projects"/"Project Summary"/"PF Project Schedule" present, "Award to PM Handoff"/"mod-pm-handoff" absent, prior features intact (`functions/api/contacts.js` in tree + `findDuplicate` in that file, `functions/api/project-photos.js` in tree + `pfPhotoLightbox` 3×, `design_professionals` 12×, `PF_FIELD_TEXTAREA_LABELS` 5× + `pfAutoBackfeedDP` 2×, `pfFmtPhone` 16×, Contract Recap 4 subgroups, Engineering 3 subsections, Project Photos section), this build's markers present (6 subsection headers "Shop Drawings"/"Testing"/"Staking & Layout"/"Safety"/"Mobilization Preparation"/"Mobilization to Site", office `card(4, 'Site Readiness / Project Setup'` present, standalone `card(4, 'Project Safety'` GONE from the office render path, office cards renumber gap-free 1-7 [General Info/Subcontract Agreement/Engineering & Design/Site Readiness/Project Photos/Financials/Project Closeout], safety/equipment/material override keys present + `_surfaceExtras` nested-render wiring intact, Section 4 fields SSSP/MSDS/Toolbox Talks/JSA/Hand Log Sheets/Locate Ticket present in the Safety subsection), 166 real bids (committed `bid-log.json`). STOP-sentinel (`/home/aiciv/tools/.budget-actuals-daemon-STOP`) untouched (0 bytes, mtime Jul 30 14:18, before + after; main-branch deploy, never a budget-actuals invocation), dirty data/`_headers`/sync files stashed `-u` before deploy + restored after (stash popped clean, index/functions never dirty). Rollback target `2b90a1b9` held ready (not needed — both gates passed). NEXT build base = new prod canonical `64a8d8d4` / git `d5db649` (Section 8 Project Closeout).
- Subcontract Agreement tax tweaks live (7 batched display-layer changes): in the office Subcontract Agreement section — (1) the Bid Recap "Bid Sent to GC" link field is REMOVED so "Released to Start Submittals" is the last Bid Recap field; (2) the Contract Recap "Sales Tax" field becomes a Yes / No-Exempt dropdown (`PF_FIELD_DROPDOWNS` entry `'Sales Tax': ['Yes', 'No-Exempt']`) with an ADJACENT new "Sales Tax %" field for the captured rate; (3) "Taxes/Sales Tax Notes" is RENAMED to "Sales Tax Notes" with a backward-compat override alias (`PF_OVERRIDE_LABEL_ALIASES = { 'Sales Tax Notes': 'Taxes/Sales Tax Notes' }` so any stored override under the old label still renders); and (4-7) four parked fields are DELETED from the "To Be Placed" holding group — "Surveying & Staking (who performs)" (redundant with the Scope-of-Work "Staking & Layout Included?" field), "Anticipated Spoils (CY)" (now lives in General Info as "Estimated Spoils (CY)"), "Date COI Sent to GC" and "Date W9 Sent to GC" (superseded by the COI/W9 tracking in "Needed for Contract Completion"). "To Be Placed" now holds exactly 4 fields (Tax Exempt? / Tax, Tax Status (per subcontract), If not Tax Exempt Sales Tax Rate, Surveyor). `index.html` display layer only (+39/-24). Shipped from `subcontract-tax-tweaks-20260807` @ `c21a02c` (base `dp-editable-backfeed-20260807` = prior prod canonical `24b966b0`; `git merge-base --is-ancestor b37b024 c21a02c` = YES, `c4b4406` feature tip and `85f4499` structural rename both ancestors = YES, so no Design-Professionals / Engineering / Contract Recap / Bid Recap / contact-directory / section regression). New prod canonical `94640440` (env=production, branch=main, 2026-08-07T18:27:29Z). Prod auth fully gated post-deploy (all four checked): `/api/me` 401, `/` 401 (Basic realm "PF Operations Platform"), `/api/contacts` 401 (body "Authentication required", not open/500), `/api/project-override` 401 (not open/500). Verified against shipped committed bytes (disk `index.html` byte-identical to `c21a02c:platform/index.html`, blob `75aa2b0d`, canonical=production confirmed): "Dead Projects"/"Project Summary"/"PF Project Schedule" present, "Award to PM Handoff"/"mod-pm-handoff" absent, prior features intact (`functions/api/contacts.js` in tree, `project-override.js` `design_professionals` key, `contacts.js` `findDuplicate`, `pfFmtPhone` 16×, Contract Recap 4 subgroups, Engineering 3 subsections, QA/QC relocated, Bid Recap "Released to Start Submittals"), this build's changes verified (live `fieldLinked('Bid Sent to GC'...)` gone = 0, remaining "Bid Sent to GC" mentions are `//` comments only; live `field('Sales Tax %')` = 1; live `field('Sales Tax Notes')` = 1 + override alias present; live `field('Taxes/Sales Tax Notes')` = 0; "Sales Tax" dropdown entry present; "To Be Placed" subgroup = exactly 4 live `field()` calls with the 4 removed fields now `//` comments; "Anticipated Spoils" not a live Contract-Recap field, "Estimated Spoils (CY)" present in General Info), 166 real bids. STOP-sentinel (`/home/aiciv/tools/.budget-actuals-daemon-STOP`) untouched (0 bytes, mtime Jul 30 14:18, before + after; main-branch deploy, never a budget-actuals invocation), dirty data/`_headers`/sync files stashed `-u` before deploy + restored after (stash popped clean). Rollback target `24b966b0` held ready (not needed — both gates passed).
- Engineering & Design restructure live (3 subsections + QA/QC merge + Section 6 removal + gap-free renumber): the office Engineering & Design section (project detail card #3) is reorganized into three blue-headered subsections — "Prelim Design", "PF Design Submittal", and "As Built Drawings" (the last split into two sub-groups, "Field As-Built Drawings" and "Stamped Final As-Built Drawings") — and gains new display-layer fields/dropdowns/links including "Design Status" and "Submittal Disposition". The standalone QA/QC section (old office Section 6, "QA / QC") is REMOVED as a top-level card and relocated INTO Engineering as a nested card under the As Built Drawings subgroup: it keeps its own `data-pr-section="qaqc"` override key (`_curSection = 'qaqc'`, `card('', 'QA / QC', qaMeta, qaBody, true, 'qaqc')`), its live installed-qty auto-progress render sourced from `q = D.qaqc` (LF / redrill / last-log intact), its Documents (`L.qaqc`), and `_surfaceExtras(... 'qaqc' ...)` — ZERO data migration, edits still save under the `qaqc` key. With the standalone card gone, the office sections renumber gap-free 1-7: General Info=1, Subcontract Agreement=2, Engineering & Design=3, Project Safety=4, Site Readiness / Project Setup=5, Financials=6 (was 7), Project Closeout=7 (was 8). The `keys` array is unchanged (the `qaqc` key persists for override continuity, now nested). Field-ops renderer (its own `card(9, 'QA / QC')`) is out of scope and preserved. `index.html` display layer only (+136/-26). Shipped from `eng-design-subsections-20260807` @ `115286a` (base `eng-section-contacts-cleanup-20260807` @ `13fe10b`/`86fe1141` = prior prod canonical `a359d5b9`; `git merge-base --is-ancestor 13fe10b 115286a` = YES, `86fe1141` and `85f4499` structural rename both ancestors = YES, so no Contract Recap / Bid Recap / contact-directory / phone / eng-cleanup / section regression). New prod canonical `294e99eb` (env=production, branch=main, 2026-08-07T17:56:08Z). Prod auth fully gated post-deploy: `/api/me` 401, `/` 401 (Basic realm "PF Operations Platform"), `/api/contacts` 401 (contact directory Function still deployed + CRM-gated, not open/500). Verified against shipped bytes: "Dead Projects"/"Project Summary"/"PF Project Schedule" present, "Award to PM Handoff"/"mod-pm-handoff" absent, prior features intact (`functions/api/contacts.js` + `/api/contacts` + "Save contact(s) to directory", `pfFmtPhone` 17×, Contract Recap 4 subgroups + "To Be Placed", Bid Recap "Released to Start Submittals" + "Bid Sent to GC", General Info "Design Professionals"), this build's markers present ("Prelim Design", "PF Design Submittal", "As Built Drawings", "Field As-Built Drawings", "Stamped Final As-Built Drawings", "Design Status", "Submittal Disposition"), QA/QC relocated (`qaqc` key + live installed-qty render intact, nested `card('', 'QA / QC', ..., 'qaqc')`), standalone office QA/QC card #6 gone (office cards renumber gap-free 1-7, Financials + Project Closeout renumbered), 166 real bids. STOP-sentinel untouched, dirty data files stashed+restored around the deploy.
- Engineering & Design Contacts list removed from standalone Section 3: the office Engineering & Design section (project detail card #3) no longer renders the "Engineering & Design Contacts" contact list — the `subhead('Engineering & Design Contacts') + contactList(g.engineering)` prefix is dropped from the section's `engBody`, leaving `var engBody = fields([...])`. Section 3 keeps its engineering tracking fields (AP Design Firm, Prelim Design Fee, Design Completed/Paid dates, etc.) + Documents and STILL owns the editable `engineering` override key (`_curSection = 'engineering'`). Engineering contacts remain viewable in the General Info "Design Professionals" block (display-only, renders from the same synced `g.engineering` source — unchanged). No contact data touched (`g.engineering` + override key intact). The separate field-ops renderer `contactList` (~L21434) is out of scope and preserved (8 `contactList` call sites remain in the bundle). `index.html` display layer only (+10/-2). Shipped from `eng-section-contacts-cleanup-20260807` @ `86fe1141` (base `contract-recap-subsections-20260807` @ `02f48a3` = prior prod canonical `e234f0b2`; `git merge-base --is-ancestor 02f48a3 86fe1141` = YES, `2759ce0` feature tip and `85f4499` structural rename both ancestors = YES, so no Contract Recap / Bid Recap / contact-directory / section regression). New prod canonical `a359d5b9` (env=production, branch=main, 2026-08-07T17:36:45Z). Prod auth fully gated post-deploy: `/api/me` 401, `/` 401 (Basic realm), `/api/contacts` 401 (contact directory Function still deployed + CRM-gated, not open/500). Verified against shipped bytes: "Dead Projects"/"Project Summary"/"PF Project Schedule" present, "Award to PM Handoff"/"mod-pm-handoff" absent, prior features intact (`functions/api/contacts.js` + `/api/contacts` + "Save contact(s) to directory", `pfFmtPhone` 17×, Contract Recap 4 subgroups "Contract Info"/"Invoicing & Payment Terms"/"Scope of Work"/"Needed for Contract Completion" + "To Be Placed", Bid Recap "Released to Start Submittals" + "Bid Sent to GC"), this build's change verified (office `engBody = fields([` with no `contactList` prefix, "Design Professionals" + `firmBlock('Ground Improvement Engineering'...)` present, Section 3 `_curSection = 'engineering'` intact), 166 real bids.
- Contract Recap 4-subsection restructure live: the Subcontract Agreement > Contract Recap subsection is reorganized into four blue-headered sub-groups — "Contract Info", "Invoicing & Payment Terms", "Scope of Work", and "Needed for Contract Completion" — and gains new display-layer fields (Contract Status dropdown, prevailing-wage hourly/fringe, Bonding Requirements, Sales Tax + tax notes, Approved SOV link, pay-app fields, staking Y/N, extra SOW, three COI date-tracking fields + email history, W9 date). The live-PDF "Contract Document (Live)" `docEmbed` iframe is removed from the office contract card (the `docEmbed` helper remains defined but is no longer invoked — 0 call sites) and replaced with an editable "Subcontract Agreement Folder (SharePoint)" link. Eight prior fields are preserved in a "To Be Placed (pending Brad's direction)" group. `index.html` display layer only (+148/-29). Shipped from `contract-recap-subsections-20260807` @ `2759ce0` (base `bid-recap-released-submittals-20260807` = prior prod canonical `66e3870a`; `git merge-base --is-ancestor 51e9e73 2759ce0` = YES and `9a87eea` feature tip = YES, `85f4499` structural rename ancestor = YES, so no Bid Recap / contact-directory / section regression). New prod canonical `e234f0b2` (env=production, branch=main, 2026-08-07T17:26:05Z). Prod auth fully gated post-deploy: `/api/me` 401, `/` 401 (Basic realm), `/api/contacts` 401 (contact directory Function still deployed + CRM-gated, body "Authentication required", not open/500). Verified against shipped bytes: "Dead Projects"/"Project Summary"/"PF Project Schedule" present, "Award to PM Handoff"/"mod-pm-handoff" absent, prior features intact (`functions/api/contacts.js` + `/api/contacts` + "Save contact(s) to directory", `pfFmtPhone` 17×, Bid Recap "Released to Start Submittals" + "Bid Sent to GC"), new markers present (4 subgroup labels "Contract Info"/"Invoicing & Payment Terms"/"Scope of Work"/"Needed for Contract Completion", "To Be Placed", "Contract Status", "Bonding Requirements", "Approved SOV", "Subcontract Agreement Folder"), "Contract document (live)" docEmbed invocation gone (0 call sites), 166 real bids.
- Contract Recap rework live: the Subcontract Agreement > Contract Recap subsection is condensed into a fixed `twoCol` left/right grouping; the "Agreement Date (Face)" field is deleted (removed from view and the section editor); "Substantial / Final Completion" is relabeled "Project Substantial Completion Date" (display label only, same `SF.completion_dates` binding and override round-trip); and "Subcontract Value" is moved to the top of the left column directly under "GC Project #". Every field stays a `.pr-field[data-pr-label]` child of the `contract` card, so `pfEditSection`/`pfSaveSection` and the `contract` override key round-trip are unchanged. `index.html` display layer only (+24/-12). Shipped from `subcontract-recap-rework-20260807` @ `bd93a1f` (base `phone-format-20260807` = prior prod canonical `6b951da7`; `git merge-base --is-ancestor 99aebf3 bd93a1f` = YES, CHANGELOG tip `91e1bed` and `85f4499` structural rename both ancestors = YES, so no section regression). New prod canonical `497959e9` (env=production, branch=main, 2026-08-07T16:11:07Z). Prod auth fully gated post-deploy: `/api/me` 401, `/` 401, `/api/contacts` 401 (contact directory Function still deployed + CRM-gated, not open/500). Verified against shipped bytes: "Dead Projects"/"Project Summary"/"PF Project Schedule" present, "Award to PM Handoff"/"mod-pm-handoff" absent, contacts (`functions/api/contacts.js` + `/api/contacts` + "Save contact(s) to directory") and `pfFmtPhone` (17×) intact, Contract Recap left-column order GC Project # -> Subcontract Value -> GC Subcontract #, old completion label gone, 166 real bids.
- Portal project-section rework live: General Info 5-subsection restructure, contact cards, MM/DD/YYYY dates, Equipment+Material into Site Readiness, Subcontract Bid/Contract Recap, standalone Engineering restored, preview-auth middleware fix. Shipped from `general-info-rework-20260806` @ `2df8ff7` (base carries current section structure; `git merge-base --is-ancestor 85f4499 HEAD` = YES). Prod auth unchanged and fully gated (the middleware bypass is preview-host-only, unreachable in production). Brad approved: "Portal changes are approved to go live with, please proceed."
- Project detail sections renumbered sequential 1-10 (gap-free): General Info=1, Subcontract Agreement=2, Engineering=3, Safety=4, Site Readiness=5, Equipment=6, Material=7, QA/QC=8, Financials=9, Closeout=10. Display numbers only (card() first argument); no section keys, data, or content changed. Shipped from `general-info-rework-20260806` @ `ef07b02`. Brad approved.
- General Info rework (office project-detail view): the standalone PF Team section was merged into General Info (data preserved via the `pfTeam` override key), General Info condensed to a responsive multi-column layout with black+bold field labels and reorganized into blue-headered subsections (Project Info -> General Contractor Info with GC contacts -> Owner -> PF Team -> Documents at bottom), and the "Contract Info" section was renamed to "Subcontract Agreement". Shipped from `general-info-rework-20260806` (base `pm-fullwidth-v2-20260806`, which carries the current section structure). Brad approved the preview.
- PM pages full-width redone on the correct base after regression rollback. The 4 PM wrapper classes (`.pf-index-root`, `.ts-root`, `.pr-root`, `.ba-wrap`) set to `max-width: none` and `.pr-root .ba-table-wrap` breakout set to `width: 100%`, shipped from `pm-fullwidth-v2-20260806` (base `financials-widen-v2-20260804`, which carries the current section structure). CSS-only; Brad approved v2 preview.

### Notes
- Recommended (pending Brad's approval to schedule): weekly D1 backup, weekly health check, monthly security review. See `docs/devops/MAINTENANCE-SUPPORT.md`.

---

## [0.9.0] — 2026-06-18 — Alpha/UAT

Per-user authentication and role-based access went live.

### Added
- **Per-user login + RBAC** backed by Cloudflare D1 (`pf-platform-db`). Three roles: `admin`, `partner`, `field_ops`. Server-side enforcement in `platform/functions/_middleware.js` with default-deny, page-level checks, and `requireArea` backstops.
- **First-login password reset** (`must_reset`) flow forcing users to set their own password.
- **Auth functions**: `login`, `logout`, `me`, `reset-password` under `platform/functions/api/`.
- **Audit log** (`audit_log` table) recording login/logout/failed-auth events.
- **Rate limiting** on auth via KV (`PF_SCHEDULE`).

### Changed
- PBKDF2 iterations set to **100,000** (Cloudflare Workers caps PBKDF2 at 100k; the earlier 210k value caused login 500s).
- `_middleware` PUBLIC_PATHS allow-list extended to clean URLs (`/login`, `/denied`, `/reset-password`) so the login page is reachable.

### Security
- **SEC-09/10:** sensitive `/data/*` files gated by role; role-aware SPA loading (field_ops cannot fetch pricing/financial data).
- **SEC-12/13/15:** stripped dollar amounts and GC identities from the field-ops schedule view; added a **build-time data-classification guard** (`migrations/check-data-classification.mjs`) to block restricted data from reaching field-facing surfaces.
- Migration files (password hashes) and `*.toml` excluded from the public deploy; D1 schema/hashes never served to the web root.
- Shared Basic-Auth gate retained as a no-lockout fallback until every user has logged in per-user.

---

## [0.8.0] — 2026-06-17 — Alpha

Major portal rebuild: a unified, navigable operations platform (~58 module panels).

### Added
- **8-header collapsible navigation skeleton** (information architecture per Brad's 2026-06-17 vision) + Project Record schema.
- **Project records for all 15 awarded projects**, with per-project detail views populated from live SharePoint data; several deepened to POET level with extracted subcontracts.
- **Per-project Subcontract Analysis** section with execution-status badges; standalone subcontract tab retired.
- **Preconstruction pipeline** views populated from the Bid Log, bucketed by Bid Log section, with full columns (AP 34 / Helical 33) and Bid-Status grouping.
- **Field Operations area**: field-facing Projects view (operational data, zero financials), Awarded Projects index, Insurance/PF COI, TimeSheets module (weekly hours by cost code + job) and annual TimeSheets summary.
- **Financials**: Budget vs Actual job-cost tracker (POET) under Project Management.
- **Auto percent-complete** computed from GUHMA column logs (LF drives completion).
- **Inline live-document embeds** via an auth-gated, drive-restricted `/api/doc` proxy.
- **PF Dashboard spec** (16 KPIs, weekly/monthly/annual toggle).

### Changed
- Renamed: Subcontractors → Vendors; Active Projects → Active Projects Summary; Project History → Projects Completed.
- Removed standalone Change Orders, Closeout, and Permits sections (per Brad).
- Weekly live SharePoint data refresh (Bid Log, Project Master, Estimating, BD).

### Docs
- Portal-rebuild SRS + SOW (11 modules), manual/training "What's New" sections, full 7-phase project-workflow SOP, and the platform-rebuild vision/roadmap.

---

## [0.5.0] — 2026-06-02 — Alpha

Security hardening of the platform after a three-agent code triple-check.

### Security
- **SEC-001:** session token now HMAC-SHA256 **signed** (`PF_TOKEN_SECRET`); forged tokens rejected.
- **SEC-002:** all `/api/*` routes require auth; removed wildcard CORS.
- **SEC-003:** credentials/secret read only from env vars, **fail-closed** (HTTP 500 if missing) — no hardcoded fallback.
- **SEC-004:** stored-XSS fixed — all data-derived values HTML-escaped via shared `window.esc` (132 sites).
- **SEC-005:** authenticated responses marked `private, no-store` (no shared-CDN caching of financial data).
- **SEC-008:** verbose error details no longer returned to the client.
- Verified live with curl/headless tests (no-auth → 401, forged token → 401, valid → 200). See `docs/platform-security/SRS.md`.

---

## [0.1.0] — 2026-05-30 — Pre-Alpha → Alpha

Initial unified platform build.

### Added
- Single-page operations platform shell (sidebar nav, shared CSS design system, modular JS) on Cloudflare Pages.
- Core modules built from Jonathan's real files: Dashboard, Feasibility, Bid Pipeline, Material Costs, Estimating, Active Projects, QA/QC GUHMA, Modulus Testing, Proposals, and supporting modules.
- Release-stage framework (Pre-Alpha → Alpha → UAT → Beta → Production) with review checklists.

---

[Unreleased]: https://github.com/peter-pf/PF-Platform/compare/v0.9.0...HEAD
[0.9.0]: https://github.com/peter-pf/PF-Platform/releases/tag/v0.9.0
[0.8.0]: https://github.com/peter-pf/PF-Platform/releases/tag/v0.8.0
[0.5.0]: https://github.com/peter-pf/PF-Platform/releases/tag/v0.5.0
[0.1.0]: https://github.com/peter-pf/PF-Platform/releases/tag/v0.1.0
