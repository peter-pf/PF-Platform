.DS_Store
node_modules/
.env
*.log
platform/sync/downloads/
docs/HANDOFF*.md
.wrangler/
__pycache__/

# Heavy raw website assets (originals, gdrive downloads, source videos) - too big for GitHub
website/assets/
backups/
backups/*.sql

# Per-user password hashes + salts. NEVER track or deploy. Local-only for D1 re-seeds.
platform/migrations/0002_seed_users.sql
